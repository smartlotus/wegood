# 项目状态（唯一给人看的状态页）

> 规则：只反映"现在"，一屏以内。过时内容移入 `.agents/handoffs/` 归档，不要让本页长成日志。
> 最后更新：<日期 时间> by <Agent 名>

## 现在

- 正在跑：<环节 / run_id / PID / 预计完成时间>，或"空闲"
- 上一结果：<环节 / verdict / 报告路径>

## 流水线全景

| 环节 | 状态 | run_id | 验收票 |
|---|---|---|---|
| A | PASS / FAIL / RUNNING / dirty | <id> | <report 路径> |
| B | … | | |

（与 pipeline_state.json 同步；冲突时以 pipeline_state.json 为准）

## 下一步

1. <具体到命令>

## 阻塞 / 待人决定

- <无则写"无">
